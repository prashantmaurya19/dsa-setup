    int n = in.nextInt(), k = in.nextInt(), d = in.nextInt(), w = in.nextInt();
    Integer[] t = new Integer[n];
    debugln(n, k, d, w, t);
    for (int i = 0, does = k, pack_open = -1, wait_start = -1, patient = -1; i < t.length; i++) {
      t[i] = in.nextInt();
      if (wait_start == -1) {
        wait_start = t[i];
        patient = i;
      } else if (wait_start + w < t[i]) {
        pack_open = wait_start + w;
        does = i;
      }
    }
